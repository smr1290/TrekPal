# Copy to .env.local for local Next.js (never commit secrets).
# Production (Vercel): set this in the project Environment Variables dashboard.
# See docs/DEPLOY.md

NEXT_PUBLIC_API_URL=http://localhost:8000
